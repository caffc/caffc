module caffc

interface obj {
}

interface HasCompare {
  compare(HasCompare other) -> i32
}

interface HasEquals {
  equals(HasEquals other) -> bool
}

interface HasHash extends HasEquals {
  hash() -> i32
}