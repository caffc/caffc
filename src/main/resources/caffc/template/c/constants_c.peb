#include "module_caffc.h"

{# we need to include all the modules to have the `module_init` and global
   vars definitions avalable -#}
{%- for module in program.modules.values() %}
#include "{{ module | c_global_header }}"
{%- endfor %}

{% include "caffc/template/c/constants_c_content.peb" %}
